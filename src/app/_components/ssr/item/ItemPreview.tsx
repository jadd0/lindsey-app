import { Item } from '@/app/_shared/types';

export default function ItemPreview(Item: Item) {
	return (
		<div className="flex flex-col items-center border border-white-800">
      
    </div>
	);
}
